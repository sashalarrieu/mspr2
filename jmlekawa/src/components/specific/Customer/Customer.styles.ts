import { Text } from 'react-native'
import { Card } from 'react-native-paper'
import styled from 'styled-components'

export const CustomerPrice = styled(Text)`
	text-align: right;
`

export const CustomerCard = styled(Card)`
	margin: 10px 5%;
`
